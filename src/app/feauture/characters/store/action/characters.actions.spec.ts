import * as CharactersActions from './characters.actions';

describe('Characters', () => {
  it('should create an instance', () => {
    expect(new CharactersActions.LoadCharacterss()).toBeTruthy();
  });
});
